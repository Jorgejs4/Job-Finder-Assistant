import {NextResponse} from "next/server";import {supabaseServer} from "../../../lib/supabase-server";
function n(v:unknown){if(v==null||String(v).trim()==="")return null;const x=Number(v);return Number.isFinite(x)?x:null}
export async function GET(req:Request){const s=await supabaseServer();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"No autenticado"},{status:401});const p=new URL(req.url).searchParams,limit=Math.min(Math.max(Number(p.get("limit")||20),1),50),offset=Math.max(Number(p.get("offset")||0),0),archived=p.get("archived")==="true",search=(p.get("search")||"").toLowerCase().trim(),status=p.get("status")||"Todos",mode=p.get("mode")||"Todos",minMatch=n(p.get("minMatch"))??0,minSalary=n(p.get("minSalary"))??0,maxSalary=n(p.get("maxSalary"))??Number.MAX_SAFE_INTEGER,sort=p.get("sort")||"match";const all:any[]=[];for(let start=0;;start+=500){const{data,error}=await s.from("jobs").select("id,title,company,canonical_url,location,status,source,description,date_posted,analysis,raw_data,created_at").eq("user_id",user.id).eq("archived",archived).order("created_at",{ascending:false}).range(start,start+499);if(error)return NextResponse.json({error:error.message},{status:400});all.push(...(data||[]));if(!data||data.length<500)break}let rows=all.map((j:any)=>{const x={...(j.raw_data||{}),...(j.analysis||{})};return{...j,match_score:Number(x.match_score)||0,salary_num:Number(String(x.salary||x.salary_min||0).replace(/[^0-9.]/g,""))||0,work_mode:x.work_mode||"No especificado",experience:Number(x.required_experience??x.experience_hint)||0}}).filter((j:any)=>(!search||`${j.title} ${j.company} ${j.location}`.toLowerCase().includes(search))&&(status==="Todos"||j.status===status)&&(mode==="Todos"||j.work_mode===mode)&&j.match_score>=minMatch&&j.salary_num>=minSalary&&j.salary_num<=maxSalary);rows.sort((a:any,b:any)=>sort==="salary_desc"?b.salary_num-a.salary_num:sort==="salary_asc"?a.salary_num-b.salary_num:sort==="date"?String(b.created_at).localeCompare(String(a.created_at)):b.match_score-a.match_score);return NextResponse.json({jobs:rows.slice(offset,offset+limit),total:rows.length,offset,limit})}
