import { NextResponse } from "next/server";
function esc(v:string){return v.replace(/([\\,;])/g,"\\$1").replace(/\r?\n/g,"\\n")}
export async function GET(req:Request){const p=new URL(req.url).searchParams,title=p.get("title")||"Entrevista",company=p.get("company")||"",link=p.get("link")||"",start=new Date(p.get("start")||Date.now());if(Number.isNaN(start.getTime()))return NextResponse.json({error:"Fecha no válida"},{status:400});const end=new Date(start.getTime()+3600000),fmt=(d:Date)=>d.toISOString().replace(/[-:]/g,"").replace(/\.\d{3}Z$/, "Z");const ics=["BEGIN:VCALENDAR","VERSION:2.0","PRODID:-//JobFinder//ES","BEGIN:VEVENT",`DTSTART:${fmt(start)}`,`DTEND:${fmt(end)}`,`SUMMARY:${esc(`Entrevista: ${title} @ ${company}`)}`,`DESCRIPTION:${esc(`Preparar entrevista. Oferta: ${link}`)}`,`URL:${esc(link)}`,"END:VEVENT","END:VCALENDAR"].join("\r\n");return new NextResponse(ics,{headers:{"content-type":"text/calendar; charset=utf-8","content-disposition":"attachment; filename=entrevista.ics"}})}
